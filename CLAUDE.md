CRITICAL: ALL test files MUST be placed in the `tests/` directory. NEVER create test files at the project root.
- Correct: `tests/test_foo.lua`
- WRONG: `test_foo.lua` (root level)

CRITICAL: When coding, NEVER add comments to code, unless absolutely necessary for understanding, such as with complicated RegExp
